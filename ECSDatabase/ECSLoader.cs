using ECS.Entities;

namespace ECSDatabase;

public class ECSLoader
{
    public static List<Entity> Load(List<Type> requestedComponents, List<Type> excludedComponents)
    {
        return new List<Entity>();
    }
}