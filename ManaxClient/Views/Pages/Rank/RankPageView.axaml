<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:page="clr-namespace:ManaxClient.ViewModels.Pages.Rank"
    xmlns:viewModels="clr-namespace:ManaxClient.ViewModels"
    xmlns:rank="clr-namespace:ManaxClient.Models.Rank"
    mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
    x:Class="ManaxClient.Views.Pages.Rank.RankPageView"
    x:DataType="page:RankPageViewModel">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" Spacing="24">

            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Classes="title" Text="Gestion des rangs" />
                    <TextBlock Text="{Binding Source={x:Static rank:Rank.Ranks}, Path=Count, StringFormat='🏆 {0} rang(s) configuré(s)'}"
                               Foreground="{StaticResource SecondaryBrush}" />
                </StackPanel>

                <Button Grid.Column="1"
                        IsVisible="{Binding $parent[Window].((viewModels:MainWindowViewModel)DataContext).CanWriteRanks}"
                        Classes="primary"
                        Command="{Binding CreateRank}"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="➕" />
                        <TextBlock Text="Nouveau rang" />
                    </StackPanel>
                </Button>
            </Grid>

            <Border Classes="card" IsVisible="{Binding Source={x:Static rank:Rank.Ranks}, Path=!Count}">
                <StackPanel Orientation="Vertical"
                            Spacing="16"
                            HorizontalAlignment="Center">
                    <TextBlock Text="🏆" FontSize="48" HorizontalAlignment="Center" />
                    <TextBlock Text="Aucun rang configuré"
                               FontSize="18"
                               FontWeight="Medium"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Créez des rangs pour organiser vos utilisateurs par niveau."
                               TextAlignment="Center"
                               Foreground="{StaticResource SecondaryBrush}" />
                    <Button Classes="primary"
                            IsVisible="{Binding $parent[Window].((viewModels:MainWindowViewModel)DataContext).CanWriteRanks}"
                            Command="{Binding CreateRank}"
                            HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="➕" />
                            <TextBlock Text="Créer le premier rang" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <Border Classes="card" IsVisible="{Binding Source={x:Static rank:Rank.Ranks}, Path=Count}">
                <StackPanel Spacing="16">
                    <TextBlock Classes="subtitle" Text="Liste des rangs" />

                    <ItemsControl ItemsSource="{Binding Source={x:Static rank:Rank.Ranks}}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="rank:Rank">
                                <Border Background="{DynamicResource Background1}"
                                        BorderBrush="{DynamicResource Background2}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="16"
                                        Margin="0,4">
                                    <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="16">

                                        <Border Grid.Column="0"
                                                Background="{StaticResource WarningBrush}"
                                                CornerRadius="20"
                                                Width="40"
                                                Height="40">
                                            <TextBlock Text="🏆"
                                                       FontSize="16"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center" />
                                        </Border>

                                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="Bold"
                                                       FontSize="16" />
                                            <TextBlock Text="{Binding Value, StringFormat='Niveau: {0}'}"
                                                       Foreground="{StaticResource SecondaryBrush}"
                                                       FontSize="12" />
                                        </StackPanel>

                                        <Border Grid.Column="2"
                                                Background="{StaticResource SuccessBrush}"
                                                CornerRadius="12"
                                                Padding="8,4"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Value}"
                                                       Foreground="{DynamicResource TextColor1}"
                                                       FontSize="12"
                                                       FontWeight="Bold" />
                                        </Border>

                                        <Button Grid.Column="3"
                                                IsVisible="{Binding $parent[Window].((viewModels:MainWindowViewModel)DataContext).CanWriteRanks}"
                                                Classes="secondary"
                                                Command="{Binding $parent[UserControl].((page:RankPageViewModel)DataContext).UpdateRank}"
                                                CommandParameter="{Binding .}"
                                                Padding="8"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="✏️" />
                                        </Button>

                                        <Button Grid.Column="4"
                                                IsVisible="{Binding $parent[Window].((viewModels:MainWindowViewModel)DataContext).CanDeleteRanks}"
                                                Classes="danger"
                                                Command="{Binding $parent[UserControl].((page:RankPageViewModel)DataContext).DeleteRank}"
                                                CommandParameter="{Binding .}"
                                                Padding="8"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="🗑️" />
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>